README
--------

sample app for easy testing of cloud infrastructure

not many packages to reduce docker build time ~15mins

and no api, keys, etc. sys.getenv used to test if environ variables can be used when running aws app runner

public repo so no keys involved